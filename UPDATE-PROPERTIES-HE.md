# עדכון נכסים באתר V3

כל הנכסים באתר נמצאים בקובץ `assets/properties-v3.js`.

## החלפת נכס קיים

1. העלו את התמונה החדשה לתיקיית `assets` בגיטהאב.
2. פתחו את `assets/properties-v3.js` ולחצו על אייקון העריכה.
3. עדכנו בנכס הרצוי את השדות `title`, `location`, `image`, `alt` ו־`specs`.
4. לחצו על `Commit changes`.

## הוספת נכס

העתיקו אחד מהבלוקים שבין `{` ל־`}` בקובץ, הדביקו אותו בסוף הרשימה, ועדכנו את הפרטים והתמונה.

אין צורך להוסיף מחיר: אזור הנכסים ב־V3 אינו מציג מחירים.
